{
  "name": "@pipeworx/mcp-macvendors",
  "version": "0.1.0",
  "description": "macvendors.com MCP — MAC address (OUI) → hardware manufacturer lookup.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "keywords": ["mcp", "mcp-server", "model-context-protocol", "pipeworx", "macvendors"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/pipeworx-io/mcp-macvendors"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
